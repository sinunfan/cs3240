generate:    
    this program is generate 1000 - 750 line of letters
    each line has 1-9 words
    each words has 1-6 letters

table:
    table will take some input from user and print into the txt file as a table (it has to be "word space word sapce" order).

my_print:
    write the output to a txt file called a1.txt using open() and write().
    

this program will not clean the txt file by itself so you have to type ": > a1.txt"

clean command is for clean all the thing in this program.